# Engineering And Architecture Rules

- Prefer version-controlled schema, config, and infrastructure changes over manual dashboard-only edits.
- Do not duplicate business logic across frontend, backend, jobs, and scripts.
- Make service boundaries, job boundaries, and integration boundaries explicit.
- Avoid framework-specific assumptions unless the project context justifies them.
- Do not assume Node.js and TypeScript are the only valid backend runtime.
