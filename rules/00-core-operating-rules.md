# Core Operating Rules

- Use the narrowest skill that matches the task; broad intake happens only through `product-intake-processor`.
- Keep frontend, backend, auth, data, and environment concerns explicitly separated.
- Prefer modular, composable systems over framework-coupled shortcuts.
- Do not overengineer early versions or small internal tools.
- Make runtime and environment assumptions explicit before designing implementation details.
