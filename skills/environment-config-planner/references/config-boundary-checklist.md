# Config Boundary Checklist

- which values are required in local development
- which values differ in CI, staging, and production
- which values are secrets versus safe non-secrets
- which values must be validated at startup
- which values must never be available in client bundles
